<!-- partials are code snippets that can be reused on multiple pages -->
<%- include('partials/header') -%>
  <div class="container">
    <div class="row justify-content-center mt-5">
      <ul class="row list-unstyled">
        <!-- looping through each Posts that is in the DB, -->
        <!-- This is an embedded JavaScript loop that iterates over an array of posts. For each post in the array, it generates the HTML inside the loop. The loop uses the variable i to keep track of the current post being processed. -->
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <!-- This is an anchor tag (<a>) that creates a link to view a specific post. The href attribute of the anchor is dynamically generated using the posts[i]._id value. This suggests that clicking on this link will take the user to a specific post's page, with the post's unique ID included in the URL. -->
            <a href="/post/<%= posts[i]._id%>">   
              <!-- Inside the anchor, there's an image tag (<img>) that displays an image associated with the current post. The src attribute is dynamically set using the posts[i].image value. This means that each post in the loop will display its respective image. -->
              <img class="img-fluid" src="<%= posts[i].image%>">
            </a>
          </li>
        <% } %>
        </ul>
    </div>
  </div>  
<%- include('partials/footer') -%>